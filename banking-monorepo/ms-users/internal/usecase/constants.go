package usecase

const (
	ErrUnexpectedSigningMethodMsg = "unexpected signing method: %v"
	
	ErrUnauthorizedUpdateMsg = "unauthorized: cannot update another user's profile"
	
	ErrUnauthorizedDeleteMsg = "unauthorized: cannot delete another user's profile"
)
